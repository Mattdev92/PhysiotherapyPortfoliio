import { FC } from 'react';
import { StyledText } from './logo.styles';

const Logo: FC = () => (
  <StyledText
    content={'FIZJO.B'}
    fontSize={'S'}
    fontWeight={'M'}
    margin={'0'}
  />
);

export default Logo;
